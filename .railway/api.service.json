{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "packages/shared/**",
      "packages/server/api/**"
    ],
    "buildCommand": "echo 'Installing dependencies...' && YARN_ENABLE_IMMUTABLE_INSTALLS=false yarn install && echo 'Building shared package (prebuild only)...' && cd packages/shared && node prebuild.js && echo 'Created shared dist:' && ls -la dist && echo 'Building API package...' && cd ../../packages/server/api && echo 'Running API build with skipLibCheck...' && tsc --skipLibCheck || { echo 'API package build failed! Checking for errors:'; grep -A 5 'error ' yarn-debug.log || echo 'No yarn-debug.log found'; exit 1; } && echo 'Build completed successfully - checking API output:' && ls -la dist"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "node packages/server/api/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
} 