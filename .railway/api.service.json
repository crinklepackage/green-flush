{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "packages/shared/**",
      "packages/server/api/**"
    ],
    "buildCommand": "echo 'Installing dependencies...' && yarn install && echo 'Building shared package...' && cd packages/shared && node prebuild.js && npx tsc && echo 'Checking shared output...' && ls -la dist && cd ../../packages/server/api && echo 'Building API package...' && npx tsc && echo 'Checking API output...' && ls -la dist"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "node packages/server/api/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
} 