{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "packages/shared/**",
      "packages/server/api/**"
    ],
    "buildCommand": "yarn workspaces focus @wavenotes-new/shared @wavenotes-new/api && echo 'Building from root with project references...' && yarn exec tsc --build --verbose && ls -la packages/shared/dist && ls -la packages/server/api/dist"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "node packages/server/api/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
} 