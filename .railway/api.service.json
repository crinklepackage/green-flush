{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "packages/shared/**",
      "packages/server/api/**"
    ],
    "buildCommand": "yarn workspaces focus @wavenotes-new/shared @wavenotes-new/api && cd packages/shared && yarn build && cd ../../packages/server/api && yarn build && echo 'Build completed'"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "node packages/server/api/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
} 