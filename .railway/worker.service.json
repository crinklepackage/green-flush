{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "packages/shared/**",
      "packages/server/api/**",
      "packages/server/worker/**"
    ],
    "buildCommand": "yarn workspaces focus @wavenotes-new/shared @wavenotes-new/api @wavenotes-new/worker && cd packages/shared && yarn build && cd ../../packages/server/api && yarn build && cd ../worker && yarn build && echo 'Build completed'"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "node packages/server/worker/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
} 